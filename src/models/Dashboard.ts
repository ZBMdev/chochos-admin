/* eslint-disable*/
/*
import { DashboardSummary } from "@/types/dashboard";

import Model from "./Model";

export default class Dashboard extends Model {
    registeredUsers!: number;
    activeUsers!:     number;
    blockedUsers!:    number;
    flaggedUsers!:    number;
    publicTopics!:    number;
    privateTopics!:   number;

    constructor(data?: Partial<DashboardSummary>) {
        super();
        if (data) {
            Object.assign(this, data);
        }
    }
}
*/